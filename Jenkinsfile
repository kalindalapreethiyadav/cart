pipeline {
    agent any
    stages {
       stage('Lint check') {
          steps {
            sh "started lint check"
            sh "ls -lrt"
            sh "pwd"
           // sh "~/node_modules/jslint/bin/jslint.js server.js"
          }
       }
    }
}